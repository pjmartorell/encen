%h1 Usuaris del sistema

%table
  %tr
    %th Nom d'usuari
    %th Accions
  - @users.each do |user|
    %tr
      %td= user.login
      %td
        = link_to "Editar", edit_admin_user_path(user)
        |
        = link_to "Eliminar", [:admin, user], :method => :delete, :confirm => "N'estàs segur?"

%hr
%p
  = link_to "Nou usuari", new_admin_user_path
